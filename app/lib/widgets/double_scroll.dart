
    // List<Widget> slivers = [];
    // for (int i = 0; i < _children.length; ++i) {
    //   final item = _children[i];
    //   slivers.add(SliverToBoxAdapter(key: ValueKey(item), child: widget.builder(context, i)));
    // }
    // // return Container();
    // final a = SliverToBoxAdapter(
    //   child: Container(
    //     height: 1000,
    //     color: Colors.teal,
    //   ),
    // );
    // // get height of a

    // final c = ConstrainedBox(
    //   // shrinkwrap
    //   constraints: BoxConstraints.tightFor(
    //     height: 1000,
    //     width: MediaQuery.of(context).size.width,
    //   ),
    //   child: Container(
    //     height: 500,
    //     color: Colors.teal,
    //   ),
    // );

    // final constraints = SliverConstraints(
    //   axisDirection: AxisDirection.down,
    //   growthDirection: GrowthDirection.forward,
    //   userScrollDirection: ScrollDirection.forward,
    //   scrollOffset: 0.0,
    //   precedingScrollExtent: 0.0,
    //   overlap: 0.0,
    //   remainingPaintExtent: 0,
    //   crossAxisExtent: MediaQuery.of(context).size.width,
    //   crossAxisDirection: AxisDirection.right,
    //   viewportMainAxisExtent: 1000,
    //   remainingCacheExtent: 0,
    //   cacheOrigin: 0.0,
    // );

    // final widgetSize = Size(MediaQuery.of(context).size.width, 1000);

    // final RenderBox renderBox = RenderConstrainedBox(
    //   additionalConstraints: BoxConstraints.tight(widgetSize),
    //   child: c.createRenderObject(context),
    // );

    // renderBox.layout(BoxConstraints.loose(widgetSize));
    
    // // // wrap c in a RenderObjectToWidgetAdapter and get height
    // // final b = RenderObjectToWidgetAdapter<RenderBox>(
    // //   container:

    // // final testableWidget = TestableWidget(tester: tester, widget: myContainer);
    // // await testableWidget.initialize();

    // // final size = testableWidget.size;
    // // expect(size.height, 1000);

    // // final item = RenderSliverToBoxAdapter(child: )

    // final renderObject = a.createRenderObject(context);
            // // final element = a.createElement();

    // // // maxWidth: MediaQuery.of(context).size.width,
    // // // maxHeight: 1000,
    // // return Container();
    // // element.activate();
    // // final newSlot = context.
    // // final root = cont
    // // context.
    // // create dummy parent
    // // element.owner = BuildOwner(focusManager: FocusManager());

    // // final owner = WidgetsBinding.instance!.buildOwner as BuildOwner;
    // // element.mount(null, null);
    //     // // element.findRenderObject()?.layout(constraints);
    // renderObject.layout(constraints, parentUsesSize: true);
    // // a.
    // // layoutChild(constraints.scrollOffset, constraints.growthDirection);
    // // layoutChildParentData(child.parentData, constraints, child.size);
    // // super.performLayout();

                // // element.unmount();
