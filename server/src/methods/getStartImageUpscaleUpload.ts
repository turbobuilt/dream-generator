import { Request, Response } from "express";
// import { getPresignedPut } from "./publishPrompt";


export async function getStartImageUpscaleUpload(req: Request, res: Response) {
    // let uploadUrl = await getPresignedPut(imgGenRequest.taskId + ".png", "dreamgenout");
}